$kind: http-request
description: Requires HR or ADMIN role.
url: "{{baseUrl}}/leave-types/:id"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  id: string
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Delete a leave type.resources/examples
order: 3000
