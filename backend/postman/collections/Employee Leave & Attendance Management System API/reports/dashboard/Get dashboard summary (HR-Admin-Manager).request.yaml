$kind: http-request
name: Get dashboard summary (HR/Admin/Manager)
url: "{{baseUrl}}/reports/dashboard"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Get dashboard summary (HR-Admin-Manager).resources/examples
order: 1000
