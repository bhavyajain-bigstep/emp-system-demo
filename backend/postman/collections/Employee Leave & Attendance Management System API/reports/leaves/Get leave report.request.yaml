$kind: http-request
url: "{{baseUrl}}/reports/leaves?employeeId=string&departmentId=string&leaveTypeId=string&status=CANCELLED&from=string&to=string&page=1&limit=20"
method: GET
queryParams:
  employeeId: string
  departmentId: string
  leaveTypeId: string
  status: CANCELLED
  from: string
  to: string
  page: "1"
  limit: "20"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Get leave report.resources/examples
order: 2000
